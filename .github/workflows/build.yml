# This file was generated by AI and is not subject to copyright protection.
# AI-generated content is not applicable for copyright.

name: Build Android App

on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]
  workflow_dispatch:

permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-latest
    name: Build Android App v0.2.0

    steps:
    - name: Checkout repository
      uses: actions/checkout@v4

    - name: Set up JDK 17
      uses: actions/setup-java@v4
      with:
        java-version: '17'
        distribution: 'temurin'
        cache: gradle

    - name: Setup Android SDK
      uses: android-actions/setup-android@v3

    - name: Install NDK and CMake
      run: sdkmanager --install "ndk;25.1.8937393" "cmake;3.22.1"

    - name: Grant execute permission for gradlew
      run: chmod +x gradlew

    - name: Build with Gradle
      run: ./gradlew assembleDebug

    - name: Upload Debug APK
      uses: actions/upload-artifact@v4
      with:
        name: app-debug
        path: app/build/outputs/apk/debug/*.apk

    - name: Upload Native Libraries (arm64-v8a)
      uses: actions/upload-artifact@v4
      with:
        name: native-libs-arm64-v8a
        path: app/build/intermediates/stripped_native_libs/debug/out/lib/arm64-v8a/*

    - name: Upload Native Libraries (x86_64)
      uses: actions/upload-artifact@v4
      with:
        name: native-libs-x86_64
        path: app/build/intermediates/stripped_native_libs/debug/out/lib/x86_64/*
